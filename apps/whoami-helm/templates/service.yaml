apiVersion: v1
kind: Service
metadata:
  name: {{ include "whoami.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "whoami.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80